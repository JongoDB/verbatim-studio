name: Build Electron App

on:
  push:
    tags:
      - 'v*'
  pull_request:
    branches: [main]
    paths:
      - 'apps/electron/**'
      - 'packages/frontend/**'
      - 'packages/backend/**'
      - 'scripts/**'
      - '.github/workflows/build-electron.yml'
  workflow_dispatch:

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: macos-14
            arch: arm64
            platform: darwin
          # Note: macOS x64 builds skipped - all GitHub macOS runners are ARM64 now
          # Intel Mac users can use ARM64 builds via Rosetta 2
          # TODO: Re-enable these when ready for multi-platform releases
          # - os: ubuntu-latest
          #   arch: x64
          #   platform: linux
          # - os: windows-latest
          #   arch: x64
          #   platform: win32

    runs-on: ${{ matrix.os }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9

      - name: Get pnpm store directory
        shell: bash
        run: |
          echo "STORE_PATH=$(pnpm store path --silent)" >> $GITHUB_ENV

      - name: Setup pnpm cache
        uses: actions/cache@v4
        with:
          path: ${{ env.STORE_PATH }}
          key: ${{ runner.os }}-pnpm-store-${{ hashFiles('**/pnpm-lock.yaml') }}
          restore-keys: |
            ${{ runner.os }}-pnpm-store-

      # Cache Python standalone + dependencies to speed up builds
      - name: Cache Python environment
        uses: actions/cache@v4
        id: python-cache
        with:
          path: build/python-standalone
          key: ${{ runner.os }}-${{ matrix.arch }}-python-ml-${{ hashFiles('scripts/requirements-core.txt', 'scripts/requirements-ml.txt', 'scripts/install-bundled-deps.sh') }}

      - name: Install dependencies
        run: pnpm install

      - name: Download Python standalone
        if: steps.python-cache.outputs.cache-hit != 'true'
        shell: bash
        run: ./scripts/download-python-standalone.sh ${{ matrix.arch }}

      - name: Install Python dependencies (core + ML)
        if: steps.python-cache.outputs.cache-hit != 'true'
        shell: bash
        run: |
          echo "Installing all Python dependencies (this may take a while)..."
          ./scripts/install-bundled-deps.sh
        env:
          # Increase pip timeout for large packages
          PIP_DEFAULT_TIMEOUT: 300

      - name: Verify Python dependencies (strict version check)
        shell: bash
        run: |
          PYTHON_BIN="build/python-standalone/python-macos-arm64/bin/python3"
          echo "=== Installed ML packages ==="
          "$PYTHON_BIN" -m pip list | grep -E "torch|whisper|pyannote|mlx|transformers|huggingface|sentence|numpy" || true
          echo ""
          echo "=== STRICT Version Verification ==="
          FAILED=0

          # Function to check exact version
          check_version() {
            local pkg=$1
            local expected=$2
            local import_name=${3:-$1}
            local actual=$("$PYTHON_BIN" -c "import $import_name; print($import_name.__version__)" 2>/dev/null || echo "NOT_INSTALLED")
            if [ "$actual" = "$expected" ]; then
              echo "✓ $pkg: $actual"
            else
              echo "✗ $pkg: $actual (expected $expected)"
              return 1
            fi
          }

          check_version "torch" "2.8.0" "torch" || FAILED=1
          check_version "torchaudio" "2.8.0" "torchaudio" || FAILED=1
          check_version "huggingface_hub" "0.36.1" "huggingface_hub" || FAILED=1
          check_version "transformers" "4.48.0" "transformers" || FAILED=1
          check_version "numpy" "2.0.2" "numpy" || FAILED=1

          # Packages with non-standard module names
          WHISPERX_VER=$("$PYTHON_BIN" -c "import whisperx; print(whisperx.__version__)" 2>/dev/null || echo "NOT_INSTALLED")
          if [ "$WHISPERX_VER" = "3.3.4" ]; then echo "✓ whisperx: $WHISPERX_VER"; else echo "✗ whisperx: $WHISPERX_VER (expected 3.3.4)"; FAILED=1; fi

          PYANNOTE_VER=$("$PYTHON_BIN" -c "import pyannote.audio; print(pyannote.audio.__version__)" 2>/dev/null || echo "NOT_INSTALLED")
          if [ "$PYANNOTE_VER" = "3.3.2" ]; then echo "✓ pyannote.audio: $PYANNOTE_VER"; else echo "✗ pyannote.audio: $PYANNOTE_VER (expected 3.3.2)"; FAILED=1; fi

          MLX_VER=$("$PYTHON_BIN" -c "import mlx_whisper; print(mlx_whisper.__version__)" 2>/dev/null || echo "NOT_INSTALLED")
          if [ "$MLX_VER" = "0.4.3" ]; then echo "✓ mlx-whisper: $MLX_VER"; else echo "✗ mlx-whisper: $MLX_VER (expected 0.4.3)"; FAILED=1; fi

          echo ""
          if [ $FAILED -eq 1 ]; then
            echo "=== BUILD FAILED: Version mismatch detected! ==="
            echo "The installed package versions do not match the required versions."
            echo "This will cause runtime errors. Check pip dependency resolution."
            exit 1
          fi
          echo "=== All versions verified ✓ ==="

      - name: Prepare Electron resources
        shell: bash
        run: ./scripts/prepare-electron-resources.sh

      - name: Update version from git tag
        shell: bash
        run: ./scripts/update-version.sh

      - name: Build frontend
        run: pnpm --filter @verbatim/frontend build:ci

      - name: Build Electron
        run: pnpm --filter @verbatim/electron build

      - name: Package Electron app
        run: |
          if [ "${{ matrix.platform }}" = "darwin" ]; then
            # Skip signing on macOS (add CSC_LINK secret to enable)
            pnpm --filter @verbatim/electron exec electron-builder --publish never --config.mac.identity=null
          else
            pnpm --filter @verbatim/electron exec electron-builder --publish never
          fi
        shell: bash
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          # macOS code signing (when secrets are set)
          CSC_LINK: ${{ secrets.MAC_CERTS }}
          CSC_KEY_PASSWORD: ${{ secrets.MAC_CERTS_PASSWORD }}
          APPLE_ID: ${{ secrets.APPLE_ID }}
          APPLE_APP_SPECIFIC_PASSWORD: ${{ secrets.APPLE_APP_SPECIFIC_PASSWORD }}
          APPLE_TEAM_ID: ${{ secrets.APPLE_TEAM_ID }}
          # Windows code signing
          WIN_CSC_LINK: ${{ secrets.WIN_CSC_LINK }}
          WIN_CSC_KEY_PASSWORD: ${{ secrets.WIN_CSC_KEY_PASSWORD }}

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: verbatim-studio-${{ matrix.platform }}-${{ matrix.arch }}
          path: |
            dist/*.dmg
          if-no-files-found: error

  release:
    needs: build
    if: startsWith(github.ref, 'refs/tags/')
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          files: artifacts/**/*
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
