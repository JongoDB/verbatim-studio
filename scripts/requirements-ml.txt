# ML dependencies - prebundled with the Electron app
# STRICT version pins to prevent pip from pulling incompatible versions

# =============================================================================
# PyTorch ecosystem - MUST install together to ensure compatibility
# =============================================================================
torch==2.8.0
torchaudio==2.8.0
torchvision==0.23.0

# =============================================================================
# HuggingFace ecosystem - version-critical for whisperx compatibility
# =============================================================================
# huggingface_hub 1.x removed use_auth_token parameter needed by whisperx
huggingface_hub==0.36.1
# transformers 5.x requires huggingface_hub>=1.3 which breaks whisperx
# whisperx 3.3.4 requires transformers>=4.48.0
transformers==4.48.0

# =============================================================================
# Transcription engines
# =============================================================================
# MLX Whisper for Apple Silicon (fast GPU transcription)
mlx-whisper==0.4.3
# WhisperX for cross-platform transcription and alignment
whisperx==3.3.4

# =============================================================================
# Speaker diarization
# =============================================================================
pyannote.audio==3.3.2
# pyannote namespace packages - MUST be explicitly listed for --target installs
# These are normally pulled as dependencies, but --no-deps skips them
pyannote-core==5.0.0
pyannote-database==5.1.3
pyannote-pipeline==3.0.1
pyannote-metrics==3.2.1

# =============================================================================
# Vision/OCR models
# =============================================================================
qwen-vl-utils==0.0.8
accelerate==1.3.0

# =============================================================================
# Semantic search embeddings
# =============================================================================
sentence-transformers==3.4.1

# =============================================================================
# Other ML dependencies with strict pins
# =============================================================================
numpy==2.0.2
