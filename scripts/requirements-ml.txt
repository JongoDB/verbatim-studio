# ML dependencies - downloaded on first use, not bundled
# These are version-pinned for reproducibility

# PyTorch (CPU version for smaller size, users can override)
torch>=2.8.0,<2.9.0
torchaudio>=2.8.0,<2.9.0

# Whisper transcription
whisperx>=3.1.0,<4.0.0
pyannote.audio>=3.1.0,<4.0.0

# LLM inference
llama-cpp-python>=0.2.0

# Vision/OCR models
transformers>=4.45.0,<5.0.0  # 5.x requires huggingface_hub>=1.3 which breaks whisperx
qwen-vl-utils>=0.0.8
accelerate>=0.26.0
torchvision>=0.23.0,<0.24.0  # Must match torch 2.8.x

# HuggingFace Hub (pinned for whisperx/pyannote compatibility)
huggingface_hub>=0.34.0,<1.0.0  # 1.x removed use_auth_token parameter

# Embeddings for semantic search
sentence-transformers>=2.2.0

# Numpy (specific version for compatibility)
numpy>=2.0.2,<2.1.0
